const Colors = {
    headerIconColor: (a = 1) => {
        return `rgba(255, 255, 255, ${a})`;
    },
    darkBlue: (a = 1) => {
        return `rgba(41, 116, 240, ${a})`;
    },
    white: (a = 1) => {
        return `rgba(255, 255, 255, ${a})`;
    }
}

export default Colors;